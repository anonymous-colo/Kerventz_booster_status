{
  "name": "kerventz-status",
  "version": "1.0.0",
  "description": "Site de collecte de numéros WhatsApp avec dashboard",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild server/index.ts --bundle --platform=node --target=node18 --outdir=dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "serverless-http": "^2.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "engines": {
    "node": "18.x"
  },
  "type": "module"
}
